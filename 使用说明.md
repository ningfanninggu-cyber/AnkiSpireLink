# Anki Spire Link 超详细中文使用说明

本说明面向普通玩家。你不需要会写代码，也不需要理解 Slay the Spire 的 mod 原理。只要按步骤准备好 Anki、AnkiConnect、ModTheSpire 和 BaseMod，就可以在爬塔时强制自己完成真实 Anki 复习，并根据 Anki 的评分获得游戏内奖励。

## 1. 这个 mod 到底是做什么的

`Anki Spire Link` 的核心目标很简单：

你在《杀戮尖塔》里打出一张普通牌之前，游戏会暂停这张牌的结算，要求你去真正的 Anki 桌面软件里完成一张复习卡。你在 Anki 里点 `Again / Hard / Good / Easy` 以后，mod 会读取这个真实评分，然后回到游戏里结算这张牌，并根据评分给出奖励。

它不是一个把 Anki 卡片画到游戏窗口里的内置复习器。它连接的是你电脑上真正打开着的 Anki 软件。也就是说，你看到的题目、答案、按钮、牌组、复习记录，全部都来自 Anki 本体。

这个设计的好处是：

- 复习结果会进入 Anki 的真实复习记录。
- Anki 的算法、牌组、卡片模板、插件、媒体文件都照常工作。
- 你可以用自己原来的 Anki 学习环境，不需要把卡片导入游戏。
- 数学题、医学题、语言题、图片题、音频题都由 Anki 自己负责显示。
- 你可以分屏：左边 Anki，右边游戏。

这个设计的代价是：

- 必须安装并启动 Anki 桌面版。
- 必须安装 AnkiConnect 插件。
- 不能只靠 AnkiWeb、AnkiDroid、AnkiMobile 使用这个 mod。
- 游戏内不会显示 Anki 的题面和答案。

## 2. 必需组件

使用这个 mod 前，你需要准备以下内容：

- Steam 版 Slay the Spire。
- ModTheSpire。
- BaseMod。
- Anki 桌面版。
- AnkiConnect 插件。
- Anki Spire Link 本体。

推荐的 mod 启动顺序是：

1. ModTheSpire
2. BaseMod
3. Anki Spire Link
4. 其他内容 mod，例如 Downfall、StSLib 等

如果你使用 Downfall，通常还会有 `StSLib`。Anki Spire Link 本身只直接依赖 BaseMod，但和大型内容 mod 一起玩时，启动时间会被大型内容 mod 的资源加载拖长，这是正常现象。

## 3. 安装 Anki 桌面版

先确认你安装的是电脑桌面版 Anki，不是网页，也不是手机端。

推荐步骤：

1. 打开 Anki 官网：`https://apps.ankiweb.net/`
2. 下载适合你系统的 Anki 桌面版。
3. 安装并打开 Anki。
4. 确认你能正常进入牌组界面。
5. 确认你至少有一个可以复习的牌组。

如果你的牌组今天没有任何到期卡片，也没有新卡，Anki 可能不会进入复习界面。这种情况下 mod 也就检测不到你正在复习。你可以换一个有到期卡片的牌组，或者调整 Anki 的每日新卡/复习上限。

## 4. 安装 AnkiConnect 插件

AnkiConnect 是这个 mod 和 Anki 沟通的桥。没有它，游戏不知道 Anki 当前是哪张卡，也不知道你最后点了 Again、Hard、Good 还是 Easy。

安装步骤：

1. 打开 Anki 桌面版。
2. 点击顶部菜单 `工具`。
3. 点击 `插件`。
4. 点击 `获取插件`。
5. 输入 AnkiConnect 的插件代码：`2055492159`。
6. 点击确认安装。
7. 完全重启 Anki。

重启很重要。AnkiConnect 安装后通常要重启 Anki 才会真正开始监听本地接口。

## 5. 检查 AnkiConnect 是否正常工作

AnkiConnect 默认监听：

```text
http://127.0.0.1:8765
```

检查方法：

1. 先打开 Anki 桌面版。
2. 确认 AnkiConnect 已经安装并启用。
3. 打开浏览器。
4. 访问 `http://127.0.0.1:8765`。

如果一切正常，浏览器里通常会显示类似 `AnkiConnect` 或版本信息的文字。

如果浏览器打不开，常见原因是：

- Anki 没打开。
- AnkiConnect 没安装成功。
- 安装后没有重启 Anki。
- AnkiConnect 插件被禁用了。
- 安全软件阻止本机端口访问。
- AnkiConnect 配置被你改过，不再使用默认端口 `8765`。

本 mod 当前固定连接 `127.0.0.1:8765`。如果你手动改了 AnkiConnect 的 host 或 port，mod 就会连不上。普通用户建议保持 AnkiConnect 默认配置。

## 6. 安装 Anki Spire Link

### 6.1 通过 Steam 创意工坊安装

如果你是普通玩家，推荐通过创意工坊安装：

1. 订阅 ModTheSpire。
2. 订阅 BaseMod。
3. 订阅 Anki Spire Link。
4. 打开 Slay the Spire。
5. 选择通过 ModTheSpire 启动。
6. 在 mod 列表中勾选 BaseMod 和 Anki Spire Link。
7. 启动游戏。

如果你还要使用 Downfall：

1. 先只用 BaseMod + Anki Spire Link 测试一次。
2. 确认能正常打牌、复习、拿奖励。
3. 再加入 StSLib、Downfall 等大型 mod。

这样更容易判断问题来自哪里。

### 6.2 手动安装 jar

如果你拿到的是 `AnkiSpireLink.jar` 文件，可以手动放入 Slay the Spire 的 mods 文件夹。

macOS Steam 默认位置大致是：

```text
~/Library/Application Support/Steam/steamapps/common/SlayTheSpire/SlayTheSpire.app/Contents/Resources/mods
```

Windows Steam 默认位置大致是：

```text
C:\Program Files (x86)\Steam\steamapps\common\SlayTheSpire\mods
```

如果你同时有“本地 jar”和“创意工坊订阅版”，可能会出现重复加载。正式游玩时建议只保留一个版本。重复加载会导致奇怪的问题，比如 mod 出现两次、设置重复、启动异常等。

## 7. 第一次启动前的准备

推荐第一次测试时按这个顺序来：

1. 打开 Anki 桌面版。
2. 确认 AnkiConnect 可用。
3. 确认你有一个可以复习的牌组。
4. 打开 Slay the Spire。
5. 通过 ModTheSpire 启动。
6. 只勾选 BaseMod 和 Anki Spire Link。
7. 进入游戏。
8. 开一局普通角色，例如铁甲战士。

第一次不要急着和很多大型 mod 一起测试。先确认最小组合能跑通，再加入其他 mod。

## 8. 开局选择界面是什么意思

进入一局游戏后，mod 会弹出一个选择界面：

```text
Anki 联动
本局是否启用 Anki 联动？
本局使用 / 本局不用
```

两个按钮的意思是：

- `本局使用`：这一局启用 Anki 联动。你会获得 Anki 联动器遗物，之后打出普通牌会触发 Anki 复习。
- `本局不用`：这一局完全不启用。不会给你遗物，也不会拦截打牌。

这个选择只影响当前这局。它的作用是避免你每一局都被迫使用 mod，也能减少和大型内容 mod 的冲突风险。

如果你只是想正常玩一局，不想学习，就选 `本局不用`。

如果你想强制自己学习，就选 `本局使用`。

## 9. Anki 联动器遗物是什么

启用后，你会获得一个遗物，中文名叫：

```text
Anki 联动器
```

它是这个 mod 的核心开关。只要本局启用了 Anki 联动，并且角色身上有这个遗物，打出普通牌时就会触发 Anki 复习流程。

它不会替换你的原始开局遗物。它会作为额外遗物加入。

## 10. 哪些牌会触发 Anki 复习

会触发：

- 攻击牌。
- 技能牌。
- 能力牌。
- 大多数普通 mod 卡牌。

不会触发：

- 状态牌。
- 诅咒牌。

原因很简单：状态牌和诅咒牌通常不是玩家主动用来产生正常战斗效果的牌，让它们触发学习会很烦，也不符合“打出一张有效牌，完成一次复习”的设计。

## 11. 一次完整的使用流程

假设你已经选了 `本局使用`，并且进入了战斗。

完整流程如下：

1. 你在游戏里选择一张普通牌。
2. 你把这张牌打出去。
3. 这张牌暂时不会立刻结算。
4. 游戏弹出 `Anki 复习` 等待界面。
5. mod 通过 AnkiConnect 检查 Anki 当前是否有正在复习的卡片。
6. 如果你设置了牌组名，mod 会请求 Anki 打开该牌组的复习界面。
7. 如果你没有设置牌组名，你需要自己在 Anki 里进入任意牌组复习。
8. 你在 Anki 里看题。
9. 你在 Anki 里显示答案。
10. 你在 Anki 里点击 `Again / Hard / Good / Easy` 之一。
11. mod 读取到这次真实评分。
12. 游戏回到战斗。
13. 原本那张牌继续结算。
14. 根据评分，本张牌和角色获得奖励。
15. 游戏内会出现一条提示，告诉你获得了什么奖励。

这不是模拟评分。必须在 Anki 里真的完成一张复习卡。

## 12. 等待界面是不是卡死

大多数情况下不是卡死。

这个 mod 的设计目标就是强制学习，所以它不会在 5 秒或 10 秒后自动放行。只要它已经检测到 Anki 里有正在复习的卡片，它就会一直等你完成这张卡。

这对数学题、长阅读题、医学题尤其重要。你可以花很长时间认真做题，游戏不会因为时间长就自动给你跳过。

但是有一个例外：

如果 mod 一开始完全检测不到 Anki 复习卡片，它会在一段时间后显示失败提示。这个失败不是说你答题太慢，而是说它根本没看到 Anki 处于复习状态。

常见原因：

- Anki 没打开。
- AnkiConnect 没开。
- Anki 没有进入复习界面。
- 你设置的牌组名不对。
- 该牌组今天没有可复习卡。
- AnkiConnect 端口不是默认的 `8765`。

如果出现失败提示，先去 Anki 里进入一个有卡可复习的牌组，然后回游戏点 `重试`。

## 13. 设置项说明

在 BaseMod 的 mod 设置界面中，Anki Spire Link 有两个主要设置。

### 13.1 Anki 牌组名

设置名：

```text
Anki 牌组名
```

作用：

填写后，mod 会通过 AnkiConnect 请求 Anki 打开这个牌组的复习界面。

适合这种情况：

- 你每天只想用固定牌组配合爬塔。
- 你不想每次手动在 Anki 里点牌组。
- 你希望游戏触发时 Anki 自动进入指定牌组。

注意：

- 牌组名必须和 Anki 中的牌组名一致。
- 大小写、空格、标点最好完全一致。
- 如果是子牌组，通常需要使用 Anki 显示的完整层级名，例如 `父牌组::子牌组`。
- 如果填错，Anki 可能无法进入正确牌组。

如果你不确定怎么填，建议先留空。

留空时：

- mod 不会指定牌组。
- 你需要自己在 Anki 中打开想复习的牌组。
- 这对多个牌组轮流学习的人更灵活。

### 13.2 自动切换到 Anki

设置名：

```text
自动切换到 Anki
```

默认值：

```text
开启
```

作用：

在支持的平台上，触发复习时自动把 Anki 切到前台；复习完成后再尽量切回 Slay the Spire。

平台差异：

- macOS：支持自动切换焦点。
- Windows：不自动切换，需要手动 Alt+Tab 或分屏。
- Linux：不自动切换，需要手动切窗口或分屏。

如果你想手动分屏，建议关闭这个设置。

关闭后：

- 游戏仍然会弹出等待面板。
- mod 仍然会后台轮询 AnkiConnect。
- 你自己负责把 Anki 和游戏窗口摆好。
- 你在 Anki 答完后，游戏会检测到评分并继续结算。
- 不会强制来回切屏。

这就是推荐的分屏玩法。

## 14. 推荐窗口布局

### 14.1 macOS 自动切换玩法

适合喜欢“打牌 -> 自动跳 Anki -> 答完自动回游戏”的玩家。

推荐设置：

- 游戏使用窗口模式。
- Anki 保持打开。
- `自动切换到 Anki` 开启。
- 不要把游戏和 Anki 放在复杂的系统级全屏 Space 里。

流程体验：

1. 打牌。
2. Anki 被拉到前台。
3. 答题。
4. 游戏尽量回到前台。
5. 牌结算。

如果 macOS 的全屏 Space 动画让你感觉难受，可以改用普通窗口分屏，并关闭自动切换。

### 14.2 分屏玩法

适合你明确说过的这种体验：

左半屏 Anki，右半屏游戏。游戏只提示你该去答题，答完以后游戏继续。

推荐设置：

- 游戏窗口模式。
- Anki 窗口放左边。
- Slay the Spire 放右边。
- `自动切换到 Anki` 关闭。
- Anki 提前进入你想复习的牌组。

流程体验：

1. 你在右侧游戏打牌。
2. 游戏弹出等待面板。
3. 你看左侧 Anki 题目。
4. 你在 Anki 里答题。
5. 你点 Again / Hard / Good / Easy。
6. 游戏检测到评分。
7. 你回到右侧继续玩。

这个模式最稳定，也最符合“用真正 Anki 学习”的设计。

### 14.3 Windows / Linux 玩法

Windows 和 Linux 当前不会自动切换软件焦点。

推荐做法：

- 先打开 Anki。
- 先确认 AnkiConnect 正常。
- 把 Anki 和游戏窗口排好。
- 游戏触发等待后，手动切到 Anki 答题。
- 答完后手动切回游戏。

Windows 下如果你点击 Steam 里的启动按钮时游戏已经在运行，Steam 可能弹出：

```text
Game already running
```

这通常不是 Anki Spire Link 的 bug，而是 Steam 发现 Slay the Spire 已经开着了。关闭这个 Steam 弹窗即可。

## 15. 奖励机制总览

Anki 的四个评分会映射为四档奖励：

| Anki 评分 | 游戏内含义 | 本牌倍率 | 固定奖励 |
| --- | --- | --- | --- |
| Again / 重来 | 没记住，慢慢来 | x1 | 最大生命 +1 |
| Hard / 困难 | 勉强想起来 | x1 | 获得 1 能量，获得 3 格挡 |
| Good / 良好 | 正常答对 | x2 | 抽 1 张牌，获得 1 临时力量 |
| Easy / 简单 | 很熟练 | x4 | 获得 1 能量，抽 1 张牌，获得 1 临时力量和 1 临时敏捷 |

这里的“本牌倍率”只影响当前正在打出的那张牌。

倍率会影响：

- 当前牌的伤害。
- 当前牌的格挡。
- 当前牌的正数 magic number。
- 群体攻击牌的每个目标伤害。

倍率不会永久修改这张牌。打完以后，mod 会恢复牌面数值，避免出现越打越膨胀的问题。

## 16. 随机额外奖励

除了固定奖励，每次评分还会有一个低强度随机额外奖励。

可能出现的额外奖励包括：

- 获得随机遗物。
- 增加 3 到 5 点最大生命。
- 回复少量生命。
- 获得少量金币。

概率如下：

| Anki 评分 | 随机遗物 | 最大生命 +3 到 +5 | 小回复 | 小金币 |
| --- | ---: | ---: | ---: | ---: |
| Again / 重来 | 1% | 4% | 8% | 12% |
| Hard / 困难 | 3% | 6% | 10% | 14% |
| Good / 良好 | 8% | 8% | 12% | 15% |
| Easy / 简单 | 15% | 10% | 10% | 10% |

设计思路是：

- Again 也不惩罚你，因为学习本来就会忘。
- Easy 的奖励更强，但不会强到完全破坏游戏。
- 随机遗物概率较低，让它像小惊喜，而不是稳定刷资源。

## 17. 游戏内提示怎么看

复习完成后，游戏里会出现提示，例如：

```text
Anki：良好。记忆回响：本牌 x2，抽 1 张，临时力量 +1。
```

如果触发了额外奖励，还会追加类似：

```text
额外奖励：获得一件随机遗物。
```

所以你不用猜自己获得了什么。看角色附近的提示泡泡即可。

## 18. 长题、数学题、思考很久的问题怎么办

可以正常使用。

这个 mod 没有“5 秒自动放行”的机制。只要 Anki 当前卡片已经被检测到，你可以慢慢思考，直到你在 Anki 里真实点击评分。

适合：

- 数学题。
- 证明题。
- 医学题。
- 长文本阅读。
- 外语长句。
- 需要听音频的卡片。
- 需要看图片的卡片。

如果你觉得游戏像卡住了，先看 Anki 那边是不是还停在一张没有评分的卡。只要没点 Again / Hard / Good / Easy，游戏就会继续等。

## 19. 常见问题排查

### 19.1 游戏显示“没有检测到 Anki 正在复习卡片”

意思是：mod 没看到 Anki 当前有可复习卡正在显示。

请按顺序检查：

1. Anki 桌面版是否已经打开。
2. AnkiConnect 是否已经安装。
3. 安装 AnkiConnect 后是否重启过 Anki。
4. 浏览器能否打开 `http://127.0.0.1:8765`。
5. Anki 是否真的进入了复习界面，而不是只停在牌组列表。
6. 当前牌组今天是否有可复习卡。
7. mod 设置里的牌组名是否写错。
8. 如果你留空牌组名，是否已经手动在 Anki 里打开牌组复习。
9. 是否有安全软件阻止本机端口。
10. AnkiConnect 是否被你改成了其他端口。

处理建议：

1. 打开 Anki。
2. 手动进入一个有卡片的牌组复习。
3. 回到游戏。
4. 点击 `重试`。

### 19.2 点击“重试 / 打开 Anki”没看到 Anki 弹出来

先看你的系统。

macOS：

- 如果 `自动切换到 Anki` 开启，mod 会尝试打开并聚焦 Anki。
- 如果 Anki 安装路径异常，系统可能无法自动找到。
- 你可以先手动打开 Anki，再回游戏点击重试。

Windows / Linux：

- 当前不会自动切换或启动 Anki。
- 按钮主要是重试检测。
- 你需要自己打开 Anki，并手动切窗口。

### 19.3 我答完了 Anki，但游戏没有继续

可能原因：

- 你只是显示了答案，但还没点 Again / Hard / Good / Easy。
- 你点的是其他插件提供的按钮，没有写入标准 Anki review log。
- AnkiConnect 没有正常返回复习记录。
- 你在另一个 Anki 配置文件里复习，而游戏连接的是当前打开的 Anki 实例。
- Anki 卡片刚好不在正常复习流程中。

建议：

1. 在 Anki 里确认已经点击了 Again / Hard / Good / Easy。
2. 回游戏等待一小会儿。
3. 如果仍然不动，点重试。
4. 关闭并重开 Anki。
5. 确认 AnkiConnect 仍然可访问。

### 19.4 为什么我打状态牌或诅咒牌没有触发

这是正常设计。

状态牌和诅咒牌不会触发 Anki 复习。

### 19.5 为什么我的攻击没有变成高倍率

请检查：

- 你是否真的在 Anki 里点了 Good 或 Easy。
- 这张牌是否有伤害、格挡或正数 magic number。
- 这张牌是不是被其他 mod 以特殊方式结算。
- 敌人是否在你答题期间已经死亡。

如果你答题期间目标死亡，mod 会尽量保留复习奖励，但原牌可能无法正常打到原目标。这种情况不应该导致崩溃。

### 19.6 为什么启动游戏很慢

Anki Spire Link 本身很小。

如果你同时加载 Downfall、StSLib、BaseMod、大量角色、大量遗物、大量卡图，启动时会加载非常多资源，日志里会出现大量图片、卡牌、遗物、事件加载信息。这通常是大型内容 mod 的正常开销。

建议：

- 第一次排查时只开 BaseMod + Anki Spire Link。
- 确认正常后再加 Downfall。
- 不要同时开太多不必要的大型 mod。

### 19.7 为什么 Steam 弹出“Game already running”

这通常是 Steam 自己的提示：游戏已经在运行，你又尝试从 Steam 启动了一次。

这不是 Anki Spire Link 的核心逻辑错误。关闭弹窗，回到已经打开的游戏即可。

### 19.8 我不想这一局学习了怎么办

如果还没开始：

- 开局选择 `本局不用`。

如果已经选择了 `本局使用`：

- 当前版本没有中途重新弹出开局选择的按钮。
- 你可以保存退出后重新开一局并选择不用。
- 或者正常完成这局。

### 19.9 可以只用 Anki 手机端吗

不可以。

这个 mod 需要电脑上的 Anki 桌面版和 AnkiConnect。本地接口运行在电脑上，手机端 Anki 不提供这个接口。

### 19.10 可以用 AnkiWeb 吗

不可以。

AnkiWeb 是网页服务，不是本地 Anki 桌面程序。mod 读取的是本机 AnkiConnect 接口。

## 20. 和 Downfall 一起使用

这个 mod 设计上尽量不依赖原版角色，所以可以和 Downfall 一起使用。

但是 Downfall 是大型内容 mod，会加入大量角色、卡牌、遗物、事件和资源，所以启动慢一些是正常的。

推荐测试顺序：

1. 只开 BaseMod + Anki Spire Link。
2. 原版铁甲战士开局。
3. 选 `本局使用`。
4. 打出 Strike。
5. 在 Anki 答一张卡。
6. 确认奖励正常。
7. 退出。
8. 加载 StSLib + Downfall。
9. 再测试原版角色。
10. 最后测试 Downfall 角色。

如果只在 Downfall 角色中出现问题，请记录：

- 角色名。
- 打出的牌名。
- Anki 评分。
- 当时敌人状态。
- ModTheSpire 日志。

这样才容易判断是哪个 mod 的牌或 patch 产生了特殊交互。

## 21. 推荐日常使用方式

如果你想严肃学习：

1. 每天先打开 Anki。
2. 确认 AnkiConnect 可用。
3. 打开 Slay the Spire。
4. 只在自己真的准备学习时选择 `本局使用`。
5. 分屏摆好窗口。
6. 关闭自动切换焦点，减少来回跳动。
7. 每张普通牌都认真答一张 Anki。

如果你想轻度学习：

1. 设置固定牌组名。
2. 保持自动切换开启。
3. 打牌时让 mod 自动拉起 Anki。
4. 答完快速回游戏。

如果你只是想正常爬塔：

1. 开局选择 `本局不用`。
2. 这局就不会触发 Anki。

## 22. 更新 mod 时要注意什么

通过创意工坊更新：

- Steam 会自动下载新版本。
- 如果游戏已经开着，建议完全退出游戏后再更新。
- 更新后重新通过 ModTheSpire 启动。

手动 jar 更新：

- 删除旧的 `AnkiSpireLink.jar`。
- 放入新的 `AnkiSpireLink.jar`。
- 不要保留多个不同版本的 jar。

如果同时有本地 jar 和创意工坊版本，建议只留一个。

## 23. 卸载 mod 时要注意什么

建议不要在一局正在使用 Anki 联动器的存档中途卸载。

更稳妥的做法：

1. 先结束当前 run，或者开新局时选择 `本局不用`。
2. 退出游戏。
3. 取消创意工坊订阅或删除本地 jar。
4. 再重新启动游戏。

如果你直接卸载一个存档里正在持有的 mod 遗物，Slay the Spire 可能因为找不到遗物类而无法正常读取该 run。这是很多 StS mod 的通用限制。

## 24. 隐私和数据说明

这个 mod 通过本机地址 `127.0.0.1:8765` 和 AnkiConnect 通信。

它需要读取的信息包括：

- Anki 当前显示的复习卡 ID。
- 当前卡所在的牌组名。
- 该卡在本次触发后的复习评分记录。
- 评分对应的 ease 值，也就是 Again / Hard / Good / Easy。

它不需要：

- 上传你的 Anki 数据到互联网。
- 读取你的 AnkiWeb 账号密码。
- 修改你的 Anki 卡片内容。
- 创建新卡。
- 删除卡片。

不过，AnkiConnect 本身是一个本地 API 插件。只建议在你信任的电脑环境中使用，且保持默认只监听本机地址。

## 25. 快速自检清单

如果你不知道哪里错了，从这张表开始：

| 检查项 | 正常状态 |
| --- | --- |
| Anki 桌面版 | 已打开 |
| AnkiConnect | 已安装并启用 |
| `127.0.0.1:8765` | 浏览器可访问 |
| Anki 牌组 | 有可复习卡 |
| ModTheSpire | 正常启动 |
| BaseMod | 已勾选 |
| Anki Spire Link | 已勾选 |
| 开局选择 | 已选择 `本局使用` |
| 角色遗物栏 | 有 Anki 联动器 |
| 打出的牌 | 不是状态牌或诅咒牌 |
| Anki 操作 | 已点击 Again / Hard / Good / Easy |

## 26. 最推荐的新手测试流程

如果你第一次使用，照这个流程做：

1. 打开 Anki。
2. 安装并启用 AnkiConnect。
3. 浏览器访问 `http://127.0.0.1:8765`，确认有响应。
4. 在 Anki 里准备一个有可复习卡的牌组。
5. 启动 Slay the Spire。
6. 只勾选 BaseMod 和 Anki Spire Link。
7. 开一局铁甲战士。
8. 选择 `本局使用`。
9. 进入第一场战斗。
10. 打出 Strike。
11. 游戏弹出 Anki 复习等待界面。
12. 切到 Anki。
13. 完成一张复习卡。
14. 点击 Good。
15. 回游戏。
16. 确认 Strike 以 x2 结算，并看到奖励提示。
17. 再测试 Easy，确认 x4 和额外奖励提示。
18. 最后再加载 Downfall 等其他 mod。

如果这个最小流程成功，说明 mod 本体、AnkiConnect 和基础通信都没问题。

## 27. 重要概念总结

记住这几条就够了：

- 这个 mod 使用真正的 Anki，不是游戏内置 Anki。
- AnkiConnect 是必需的桥梁。
- 游戏只会等待真实 Anki 评分。
- 没有自动跳过，因为目的就是强制学习。
- 长题可以慢慢做。
- 分屏玩法建议关闭自动切换焦点。
- Windows 和 Linux 需要手动切窗口。
- 状态牌和诅咒牌不触发。
- Again 也有奖励，不会惩罚遗忘。
- Good 和 Easy 会明显增强当前打出的牌。

## 28. 外部链接

- Anki 桌面版下载：`https://apps.ankiweb.net/`
- AnkiConnect 插件页：`https://ankiweb.net/shared/info/2055492159`
- AnkiConnect 项目页：`https://github.com/FooSoft/anki-connect`

